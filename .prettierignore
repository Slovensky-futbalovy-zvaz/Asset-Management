node_modules
dist
build
.next
.turbo
coverage
pnpm-lock.yaml
*.min.js
*.min.css

# Generated
**/generated/**
**/*.generated.*

# Pagefind search index (generated by apps/docs postbuild hook)
# Gitignored, but exists locally after `pnpm build`, so prettier would otherwise
# pick it up during `pnpm format:check`.
apps/docs/public/_pagefind/

# Generated openapi-typescript output (apps/web/src/lib/api-types.ts).
# Gitignored, regenerated by the pretypecheck/prelint/prebuild hooks from
# apps/api/openapi.json. Has its own formatting style we don't want to touch.
apps/web/src/lib/api-types.ts

# Auto-generated OpenAPI spec — produced by `pnpm --filter @inventario/api openapi:export`
# with deterministic 2-space JSON.stringify formatting. Prettier would reformat
# multi-line strings inside `description` fields and break the CI freshness check.
apps/api/openapi.json
